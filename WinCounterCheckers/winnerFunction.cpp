#include "winnerFunction.h"

bool winner(bool) {
	int redCounter;
	int blackCounter;
	do {
		if (flag = 1) {
			blackCounter++;
		}
		if (flag = 0) {
			redCounter++;
		}
	} while ((redCounter != 12) || (blackCounter != 12));
	if (blackCounter == 1) {
		return 1;
		}
	if (redCounter == 0) {
		return 0;
	}
}